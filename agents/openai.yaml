interface:
  display_name: "Beads Rust"
  short_description: "Manage local-first issues and dependencies with br"
  default_prompt: "Use beads-rust to inspect the workspace, perform the requested br issue operations, verify the result, and report queue or sync impact."

policy:
  allow_implicit_invocation: true
